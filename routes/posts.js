import express from "express";
const router = express.Router();

// Get all posts
router.get("/", );

// Get a single post
router.get("/:id", );

// Create a post
router.post("/", );

// Update a post
router.put("/:id",);

// Delete post
router.delete("/:id",);

export default router;
